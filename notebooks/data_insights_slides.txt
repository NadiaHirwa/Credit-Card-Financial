---
title: "Data Insights Slides"
format:
  revealjs:
    theme: simple
    transition: slide
    slide-number: true
    toc: true
    incremental: true
    self-contained: true
    code-fold: false
    code-tools: false
    fig-align: center
    fig-cap-location: bottom
    fig-responsive: true
execute:
  echo: true
  warning: false
  message: false
---

# Introduction

Welcome to the data insights presentation. This slide deck is based on our analysis of the dataset, highlighting trends, distributions, and key statistical insights.

---

# Dataset Overview

```{python}
#| label: data-overview
import os
import pandas as pd
processed_dir = "processed_data"
merged_data_filename = os.path.join(processed_dir, "Credit_Card_Financial.csv")
merged_df = pd.read_csv(merged_data_filename)
merged_df.head()
```

---

# Summary Statistics

```{python}
merged_df.describe()
```

---

# Gender Distribution

```{python}
import plotly.express as px
fig = px.histogram(merged_df, x='Gender', title='Gender Distribution')
fig.show()
```

---

# Age Distribution

```{python}
fig = px.histogram(merged_df, x='Age', nbins=30, title='Age Distribution')
fig.show()
```

---

# Credit Limit by Gender

```{python}
fig = px.box(merged_df, x='Gender', y='Credit_Limit', title='Credit Limit by Gender')
fig.show()
```

---

# Statistical Tests: Gender & Credit Limit

```{python}
from scipy.stats import ttest_ind
men = merged_df[merged_df['Gender'] == 'Male']['Credit_Limit']
women = merged_df[merged_df['Gender'] == 'Female']['Credit_Limit']
t_stat, p_val = ttest_ind(men, women, nan_policy='omit')
print("p-value:", p_val)
```

**Interpretation:** If p-value < 0.05, there is a significant difference in credit limits between genders.

---

# Distribution: Credit Limit

```{python}
fig = px.histogram(merged_df, x="Credit_Limit", nbins=50, title="Credit Limit Distribution")
fig.show()
```

---

# Skewness and Kurtosis

```{python}
from scipy.stats import skew, kurtosis
print("Skewness:", skew(merged_df["Credit_Limit"].dropna()))
print("Kurtosis:", kurtosis(merged_df["Credit_Limit"].dropna()))
```

**Interpretation:**
- Skewness > 0: Right-skewed
- Kurtosis > 0: Leptokurtic (heavy tails)

---

# Recommendations

- Consider gender-based credit policy adjustments.
- Further investigate age-related trends.
- Handle skewness through data transformation if needed.

---

# Thank You

Questions & Feedback
